#include <stdio.h>
#include <string.h>

//strcpy(copia de valor para string), strcat(concatena strings), strcmp(compara suas string)

//Busca
//strchr(realiza a primeira ocorrencia de caractere em uma string)
//strtok(dividi uma string em substring)
//strlen(retorna o tamanho da string)

int main(){
    printf("Hello world!!");
    return 0;
}

